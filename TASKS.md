# Phase 1 (MVP) 필수 구현 업무 목록

> 출처: `ROADMAP.md` "핵심 기능 4가지". 클래스 단위가 아니라 기능(요구사항) 단위로 분해. Phase 2 이후 항목은 선택/후속 과제이므로 제외.

| 완료 | 기능 |
|---|---|
| [x] | 소스 DB 결과를 전체 메모리에 올리지 않고 커서 방식으로 순회하며 읽는다 |
| [x] | 읽은 행을 N건 단위로 묶어서 반환한다 |
| [x] | N건씩 모은 데이터를 배치(batch) insert로 타겟 DB에 쓴다 |
| [x] | 전체를 하나의 트랜잭션으로 묶지 않고, 청크 단위로 커밋한다 |
| [x] | 청크 쓰기 실패 시, 일시적 오류(커넥션 끊김, 데드락 등)와 영구적 오류(제약조건 위반 등)를 구분한다 |
| [x] | 일시적 오류만 지수 백오프를 적용해 N회 재시도한다 |
| [ ] | 청크가 성공적으로 처리될 때마다 마지막으로 처리한 위치(키)를 별도 테이블에 기록한다 |
| [ ] | Job 재시작 시, 기록된 마지막 위치를 조회해서 그 다음 지점부터 읽기를 재개한다 |
| [ ] | 마이그레이션 도중 강제로 실패시킨 뒤, 재시작이 정확히 그 지점부터 이어지는지 통합 테스트로 검증한다 |