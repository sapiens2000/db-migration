# db-migration 작업 방식

이 프로젝트는 학습이 목적인 포트폴리오 프로젝트다 (`ROADMAP.md` 참고). "원리 이해가 우선"이므로 구현 방식에 다음 규칙을 따른다.

## 페어프로그래밍 방식

- 새 기능/컴포넌트를 구현할 때 Claude가 전체를 혼자 다 짜지 않는다.
- 역할 분담: **Claude가 실패하는 테스트(RED)를 작성**하고, **사용자가 그 테스트를 통과시키는 구현(GREEN)을 직접 작성**한다.
  - Claude는 구현 코드를 대신 쓰지 않는다. 사용자가 막히면 힌트/설명으로 돕되, 코드를 직접 작성해주지 않는다.
- TDD 사이클을 한 테스트씩 진행한다:
  1. 다음에 검증할 동작을 한 문장으로 설명
  2. Claude가 테스트 작성 → 실행해서 실패(RED) 확인
  3. 사용자가 구현 작성
  4. Claude가 테스트 실행해서 통과(GREEN) 확인
  5. 다음 테스트로 넘어가기 전에 사용자에게 확인받는다
- 한 번에 여러 테스트/기능을 몰아서 구현하지 않는다 — 사용자가 각 단계를 따라올 수 있는 속도로 진행한다.

## 설계 승인 절차

- 새 컴포넌트를 만들기 전에는 (brainstorming 스킬의 bounded 경로대로) 설계를 먼저 채팅으로 제시하고 승인을 받은 뒤 구현을 시작한다.
- DataSource, 테이블 스키마 생성 방식, ID 생성 전략처럼 이후 코드에 영향을 주는 결정은 구현 전에 질문으로 확인한다.
- 설계가 확정되면 Claude가 클래스 파일에 메서드별 `// TODO` 주석으로 무엇을 해야 하는지(의도, 힌트)를 적어둔다. 이 시점에는 구현 코드를 쓰지 않는다.
- 사용자가 TODO를 보고 구현하면, Claude가 코드를 평가/피드백한다 (품질 문제, 설계와 어긋나는 부분 등). 피드백 → 수정을 필요한 만큼 반복한다.
- 최종적으로 사용자가 구현을 승인하면, Claude가 그 TODO 주석을 지우고 "왜 이렇게 했는지" 의도를 설명하는 일반 주석으로 교체한다 (WHAT이 아니라 WHY 위주 — 최상단 CLAUDE.md 코드 스타일 규칙과 동일한 기준).

## 코드 제출 (머지 전 확인)

- 구현이 끝나 머지/제출하기 전에는 `do-i-understand` 스킬을 사용해 사용자가 변경사항을 실제로 이해하고 책임질 수 있는지 확인한다.
- PR, 브랜치, diff를 열거나 제출/리뷰/머지하려는 시점에 적용한다.

## 인프라

- 로컬 테스트는 `docker-compose.yml`로 띄운 MySQL(3306, source_db)·PostgreSQL(5432, target_db) 컨테이너를 사용한다. 계정/비밀번호는 `application.yml`과 일치시킨다.
- 통합 테스트는 목(mock) 없이 실제 컨테이너 DB에 직접 붙어서 검증한다.